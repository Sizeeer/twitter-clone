// import { RootState } from "../../store";
// import { LOADING_STATE, RecommendationState } from "./contracts/reducer";

// const selectRecommendation = (state: RootState): RecommendationState =>
//   state.recommendation;

// export const selectRecommendationData = (
//   state: RootState
// ): RecommendationState["data"] | undefined => selectRecommendation(state).data;

// export const selectLoadingState = (state: RootState): boolean =>
//   selectRecommendation(state).loadingState === LOADING_STATE.LOADING;
