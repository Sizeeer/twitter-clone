// import { LOADING_STATE, TopicsState } from './contracts/reducer';
// import { createSelector } from "reselect";
// import { RootState } from '../../store';

// const selectTopics = (state: RootState): TopicsState => state.topics

// export const selectTopicsItems = createSelector(selectTopics, topics => topics.items )
// export const selectTopicsLoadingState = (state: RootState) => selectTopics(state).loadingState === LOADING_STATE.LOADING
